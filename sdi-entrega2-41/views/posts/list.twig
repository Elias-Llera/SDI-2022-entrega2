{% extends "layout.twig" %}
{% block title %} Tienda – uoMusic {% endblock %}
{% block main_container %}

    <h2>Publicaciones</h2>

    <!-- Listado de publicaciones -->
    <div class="row">
        {% for post in posts %}
            <div class="card" style="width: 18rem;">
                    <h3 class="card-title">{{ post.title }}</h3>
                    <h4 class="card-subtitle mb-2 text-muted">{{ post.author }} - {{ post.date }}</h4>
                    <p class="card-text" class="text-right">{{ post.content }} €</p>
            </div>
        {% endfor %}
    </div>

    <!-- Paginación mostrar la actual y 2 anteriores y dos siguientes -->
    <div class="row text-center">
        <ul class="pagination">
            {% for page in pages %}
                <li class="page-item" id="pi-{{ page }}">
                    <a class="page-link" href="/posts/{{ userId }}/?page={{ page }}"> {{ page }}</a>
                </li>
            {% endfor %}
            <script>
                $("#pi-" + "{{ currentPage }}").addClass("active");
            </script>
        </ul>
    </div>

{% endblock %}
